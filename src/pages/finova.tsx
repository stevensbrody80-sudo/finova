import { useEffect, useMemo, useState } from 'react';
import { Link, useLocation, useParams } from 'wouter';
import { ArrowLeft, ArrowRight, BadgeCheck, BarChart3, BookOpen, Check, CheckCircle2, ChevronDown, CircleDollarSign, Clock3, CreditCard, Flame, LockKeyhole, Play, RotateCcw, Search, ShieldCheck, Sparkles, Target, TrendingUp, WalletCards, X } from 'lucide-react';
import { AppShell, Logo } from '@/components/finova-shell';
import { categories, curriculumModules, glossary, initialMastery, initials, isMilestoneUnlocked, lessonContent, lessons, makeRoadmap, masteryLevels, readinessScore, recordActivity, roadmapBase, saveState, seedState, todayLabel, type FinovaState, type KnowledgeCheck } from '@/lib/finova';

function Button({ children, className = '', ...props }: React.ButtonHTMLAttributes<HTMLButtonElement>) { return <button className={`inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground transition-transform hover:-translate-y-0.5 disabled:cursor-not-allowed disabled:opacity-50 ${className}`} {...props}>{children}</button>; }
function OutlineButton({ children, className = '', ...props }: React.ButtonHTMLAttributes<HTMLButtonElement>) { return <button className={`inline-flex items-center justify-center gap-2 rounded-xl border border-border bg-card px-4 py-2.5 text-sm font-semibold transition-colors hover:bg-muted ${className}`} {...props}>{children}</button>; }
function ProgressBar({ value, color = 'bg-primary' }: { value: number; color?: string }) { return <div className="h-2 overflow-hidden rounded-full bg-muted"><div className={`h-full rounded-full ${color} transition-all duration-700`} style={{ width: `${Math.min(100, value)}%` }} /></div>; }
function PageIntro({ eyebrow, title, text, action }: { eyebrow?: string; title: string; text?: string; action?: React.ReactNode }) { return <div className="mb-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div>{eyebrow && <p className="mb-2 text-xs font-bold uppercase tracking-[.18em] text-primary">{eyebrow}</p>}<h1 className="serif text-3xl font-semibold tracking-tight text-foreground sm:text-4xl">{title}</h1>{text && <p className="mt-2 max-w-2xl text-sm leading-relaxed text-muted-foreground">{text}</p>}</div>{action}</div>; }
function SafetyNote() { return <p className="text-[11px] leading-relaxed text-muted-foreground">Finova is for financial education, not individualized financial, investment, tax, or legal advice.</p>; }
function ShellPage({ children, state, setState }: { children: React.ReactNode; state: FinovaState; setState: (s: FinovaState) => void }) { return <AppShell state={state}>{children}</AppShell>; }

export function Landing() {
  return <div className="min-h-[100dvh] bg-[#f6f0e4] text-[#173b40]"><header className="mx-auto flex max-w-6xl items-center justify-between px-5 py-6 sm:px-8"><Logo /><div className="flex items-center gap-3"><Link href="/profile" className="hidden text-sm text-[#173b40]/70 sm:inline" data-testid="link-landing-profile">Demo profile</Link><Link href="/onboarding" className="rounded-xl bg-[#173b40] px-4 py-2.5 text-sm font-semibold text-[#f6e9cb]" data-testid="link-get-started-top">Get started</Link></div></header><main><section className="mx-auto grid max-w-6xl items-center gap-14 px-5 pb-20 pt-12 sm:px-8 lg:grid-cols-[1.03fr_.97fr] lg:gap-20 lg:pb-28 lg:pt-16"><div className="animate-rise"><p className="mb-5 inline-flex items-center gap-2 rounded-full border border-[#c9aa70]/50 bg-[#f0dfbc]/55 px-3 py-1.5 text-xs font-semibold text-[#806230]"><Sparkles size={14} /> A calmer way to learn money</p><h1 className="serif max-w-xl text-[clamp(3.3rem,7vw,6.5rem)] leading-[.95] tracking-[-.055em]">Feel more ready for whatever comes next.</h1><p className="mt-7 max-w-lg text-lg leading-relaxed text-[#173b40]/68">Finova turns the money questions in your head into one clear next step—then helps you practice it in real life.</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/onboarding" className="inline-flex items-center gap-2 rounded-xl bg-[#1c766e] px-5 py-3.5 text-sm font-semibold text-[#f8f1e3] shadow-lg transition-transform hover:-translate-y-0.5" data-testid="link-start-assessment">Find my next step <ArrowRight size={17} /></Link><a href="#how" className="inline-flex items-center rounded-xl px-4 py-3.5 text-sm font-semibold text-[#173b40]/70 hover:bg-[#e9ddca]" data-testid="link-how-it-works">See how it works</a></div><div className="mt-9 flex items-center gap-3 text-sm text-[#173b40]/58"><div className="flex -space-x-2"><span className="grid h-8 w-8 place-items-center rounded-full border-2 border-[#f6f0e4] bg-[#e8bd72] text-xs font-bold">A</span><span className="grid h-8 w-8 place-items-center rounded-full border-2 border-[#f6f0e4] bg-[#d99175] text-xs font-bold">J</span><span className="grid h-8 w-8 place-items-center rounded-full border-2 border-[#f6f0e4] bg-[#94b4a9] text-xs font-bold">M</span></div><span>Built for the questions you are actually asking.</span></div></div><div className="relative mx-auto w-full max-w-[510px]"><div className="absolute -right-5 -top-8 h-32 w-32 rounded-full bg-[#e8bd72]/55 blur-2xl" /><div className="relative overflow-hidden rounded-[2rem] bg-[#1b5b5b] p-5 shadow-2xl"><div className="absolute -right-24 -top-20 h-64 w-64 rounded-full border-[34px] border-[#e8bd72]/25" /><div className="relative rounded-[1.5rem] bg-[#e6efe4] p-5 text-[#173b40]"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-[#1c766e]">Your money map</p><p className="mt-1 text-2xl font-semibold">A little clearer.</p></div><div className="grid h-11 w-11 place-items-center rounded-full bg-[#e8bd72]"><Target size={20} /></div></div><div className="mt-7 rounded-2xl bg-[#f8f3e8] p-4"><div className="flex items-end justify-between"><div><p className="text-xs text-[#173b40]/55">Sample readiness</p><p className="mt-1 text-4xl font-semibold">64<span className="text-lg text-[#173b40]/40">/100</span></p></div><div className="rounded-full bg-[#cce1d7] px-2.5 py-1 text-xs font-semibold text-[#1c766e]">growing</div></div><div className="mt-4 h-3 rounded-full bg-[#dde6dd]"><div className="h-full w-[64%] rounded-full bg-[#1c766e]" /></div></div><div className="mt-4 space-y-3"><div className="flex items-center gap-3 rounded-2xl bg-[#f8f3e8] p-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#f0dfbc] text-[#806230]"><ShieldCheck size={18} /></span><div className="flex-1"><p className="text-sm font-semibold">Build your buffer</p><p className="text-xs text-[#173b40]/55">6 min · next up</p></div><ArrowRight size={17} className="text-[#1c766e]" /></div><div className="flex items-center gap-3 rounded-2xl bg-[#f8f3e8] p-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#d7e9e0] text-[#1c766e]"><BookOpen size={18} /></span><div className="flex-1"><p className="text-sm font-semibold">Money basics</p><p className="text-xs text-[#173b40]/55">2 of 3 steps complete</p></div><CheckCircle2 size={17} className="text-[#1c766e]" /></div></div></div><p className="relative mt-4 text-center text-xs text-[#f6e9cb]/65">Personalized learning, without the pressure.</p></div></div></section><section id="how" className="border-y border-[#ded1bc] bg-[#f0e7d8]"><div className="mx-auto max-w-6xl px-5 py-20 sm:px-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-[#9b7040]">How it works</p><h2 className="serif mt-3 max-w-xl text-4xl leading-tight">No lectures. Just the next useful thing.</h2><div className="mt-12 grid gap-5 md:grid-cols-3"><div className="rounded-3xl bg-[#f8f3e8] p-6"><span className="serif text-5xl text-[#d99175]">01</span><h3 className="mt-8 text-xl font-semibold">Tell us what feels fuzzy</h3><p className="mt-3 text-sm leading-relaxed text-[#173b40]/65">A few honest answers create a starting point that feels like yours—not a generic checklist.</p></div><div className="rounded-3xl bg-[#173b40] p-6 text-[#f6e9cb]"><span className="serif text-5xl text-[#e8bd72]">02</span><h3 className="mt-8 text-xl font-semibold">Get a money roadmap</h3><p className="mt-3 text-sm leading-relaxed text-[#f6e9cb]/65">Your goals and confidence shape a gentle sequence of milestones you can actually follow.</p></div><div className="rounded-3xl bg-[#d8e8de] p-6"><span className="serif text-5xl text-[#1c766e]">03</span><h3 className="mt-8 text-xl font-semibold">Learn by doing</h3><p className="mt-3 text-sm leading-relaxed text-[#173b40]/65">Short lessons and life-like simulations help the idea stick before you need it.</p></div></div></div></section><section className="mx-auto grid max-w-6xl gap-10 px-5 py-20 sm:px-8 lg:grid-cols-2 lg:items-center"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#1c766e]">Made for real life</p><h2 className="serif mt-3 text-4xl leading-tight">The stuff you wish someone had explained sooner.</h2></div><div className="grid gap-3 sm:grid-cols-2"><div className="rounded-2xl border border-[#ded1bc] p-5"><CircleDollarSign className="text-[#1c766e]" size={22} /><p className="mt-7 font-semibold">Plain-language lessons</p><p className="mt-2 text-sm text-[#173b40]/60">No jargon wall. No shame spiral.</p></div><div className="rounded-2xl border border-[#ded1bc] p-5"><TrendingUp className="text-[#d08067]" size={22} /><p className="mt-7 font-semibold">Practice before pressure</p><p className="mt-2 text-sm text-[#173b40]/60">Try decisions in a safe sandbox.</p></div></div></section></main><footer className="mx-auto flex max-w-6xl flex-col gap-3 border-t border-[#ded1bc] px-5 py-8 text-xs text-[#173b40]/55 sm:flex-row sm:items-center sm:justify-between sm:px-8"><span>© {new Date().getFullYear()} Finova</span><span>Financial education, not individualized advice.</span></footer></div>;
}

const questions = [
  { id: 'name', label: 'What should we call you?', type: 'text', options: [] },
  { id: 'stage', label: 'What stage are you in?', type: 'single', options: ['College freshman', 'College sophomore', 'College junior', 'College senior', 'Recent graduate', 'Other'] },
  { id: 'confidence', label: 'How confident do you feel managing money?', type: 'single', options: ['1 — Not confident', '2', '3 — Getting there', '4', '5 — Very confident'] },
  { id: 'topics', label: 'Which topics confuse you most?', type: 'multi', options: ['Budgeting', 'Saving', 'Credit', 'Investing', 'Roth IRA', 'Taxes', 'Debt', 'Insurance'] },
  { id: 'intent', label: 'Where are you with investing?', type: 'single', options: ['I invest and understand it', 'I invest but do not fully understand it', 'I want to start but do not know how', 'Not currently interested'] },
  { id: 'savings', label: 'How much emergency savings do you have?', type: 'single', options: ['None', 'Under $500', '$500–$1,500', '$1,500–$5,000', 'More than $5,000', 'Not sure'] },
  { id: 'goal', label: 'What is your biggest financial goal right now?', type: 'single', options: ['Stop overspending', 'Build savings', 'Build credit', 'Start investing', 'Understand retirement', 'Prepare for life after college', 'Pay down debt', 'Understand money in general'] },
  { id: 'why', label: 'What money question do you most want answered?', type: 'text', options: [] },
];
export function Onboarding() {
  const [, setLocation] = useLocation();
  const [step, setStep] = useState(0);
  const [answers, setAnswers] = useState<Record<string, string | string[]>>(() => { try { return JSON.parse(localStorage.getItem('finova-onboarding-draft') || '{"topics":[]}'); } catch { return { topics: [] }; } });
  useEffect(() => { localStorage.setItem('finova-onboarding-draft', JSON.stringify(answers)); }, [answers]);
  const q = questions[step]; const current = answers[q.id];
  const canNext = q.type === 'text' ? String(current || '').trim().length > 1 : q.type === 'multi' ? (Array.isArray(current) && current.length > 0) : Boolean(current);
  const choose = (option: string) => { if (q.type === 'multi') { const arr = Array.isArray(current) ? current : []; setAnswers({ ...answers, [q.id]: arr.includes(option) ? arr.filter(x => x !== option) : [...arr, option] }); } else setAnswers({ ...answers, [q.id]: option }); };
  const savingsMap: Record<string, number> = { None: 0, 'Under $500': 250, '$500–$1,500': 1000, '$1,500–$5,000': 3000, 'More than $5,000': 6000, 'Not sure': 0 };
  const finish = () => {
    const mastery = initialMastery(answers); const roadmap = makeRoadmap(answers);
    const next = { ...seedState, onboarded: true, name: String(answers.name || 'You').trim(), stage: String(answers.stage || ''), confidence: Number(String(answers.confidence || '3').charAt(0)), savings: savingsMap[String(answers.savings)] ?? 0, goal: String(answers.goal || ''), intent: String(answers.intent || ''), interests: Array.isArray(answers.topics) ? answers.topics : [], roadmap, mastery, activity: ['You built your first Finova roadmap'] };
    next.baselineReadiness = readinessScore(next); saveState(next); localStorage.removeItem('finova-onboarding-draft'); setLocation('/roadmap-generating');
  };
  return <div className="min-h-[100dvh] bg-[#f6f0e4] text-[#173b40]"><header className="mx-auto flex max-w-3xl items-center justify-between px-5 py-6 sm:px-8"><Logo /><Link href="/" className="text-sm text-[#173b40]/60" data-testid="link-exit-onboarding">Save & exit</Link></header><main className="mx-auto max-w-2xl px-5 pb-12 pt-10 sm:px-8 sm:pt-16"><div className="flex items-center justify-between text-xs font-semibold text-[#173b40]/55"><span>YOUR MONEY MAP</span><span>{step + 1} of {questions.length}</span></div><div className="mt-3 h-1.5 rounded-full bg-[#ded1bc]"><div className="h-full rounded-full bg-[#1c766e] transition-all duration-500" style={{ width: `${((step + 1) / questions.length) * 100}%` }} /></div><div className="mt-16 animate-rise"><p className="text-sm text-[#1c766e]">A judgment-free check-in</p><h1 className="serif mt-3 text-4xl leading-tight sm:text-5xl">{q.label}</h1>{q.type === 'text' ? <textarea value={String(current || '')} onChange={e => setAnswers({ ...answers, [q.id]: e.target.value })} rows={q.id === 'name' ? 2 : 5} placeholder={q.id === 'name' ? 'Your first name is enough…' : 'Example: What should I be doing with my money right now?'} className="mt-8 w-full resize-none rounded-2xl border border-[#cdbfa9] bg-[#fbf7ef] p-4 text-base outline-none focus:ring-2 focus:ring-[#1c766e]" /> : <div className="mt-8 grid gap-3">{q.options.map(option => { const selected = q.type === 'multi' ? Array.isArray(current) && current.includes(option) : current === option; return <button key={option} onClick={() => choose(option)} className={`flex w-full items-center justify-between rounded-2xl border p-4 text-left text-sm transition-all ${selected ? 'border-[#1c766e] bg-[#d8e8de]' : 'border-[#d9cdb9] bg-[#fbf7ef] hover:border-[#9ebaae]'}`}><span>{option}</span><span className={`grid h-5 w-5 place-items-center rounded-full border ${selected ? 'border-[#1c766e] bg-[#1c766e] text-white' : 'border-[#b8aa96]'}`}>{selected && <Check size={13} />}</span></button>})}</div>}{q.type === 'multi' && <p className="mt-3 text-xs text-[#173b40]/55">Choose as many as you like.</p>}</div><div className="mt-14 flex items-center justify-between"><OutlineButton onClick={() => step > 0 && setStep(step - 1)} disabled={step === 0}><ArrowLeft size={16} />Back</OutlineButton>{step === questions.length - 1 ? <Button onClick={finish} disabled={!canNext}>Build my roadmap <ArrowRight size={16} /></Button> : <Button onClick={() => setStep(step + 1)} disabled={!canNext}>Continue <ArrowRight size={16} /></Button>}</div><p className="mt-10 text-center text-xs text-[#173b40]/50">Your answers stay in this browser for this prototype.</p></main></div>;
}

export function Generating() {
  const [, setLocation] = useLocation(); const [index, setIndex] = useState(0); const steps = ['Reading your answers', 'Finding your best first win', 'Laying out your money map'];
  useEffect(() => { const timer = setInterval(() => setIndex(i => Math.min(steps.length - 1, i + 1)), 800); const done = setTimeout(() => setLocation('/dashboard'), 2800); return () => { clearInterval(timer); clearTimeout(done); }; }, [setLocation]);
  return <div className="grid min-h-[100dvh] place-items-center bg-[#173b40] px-5 text-[#f6e9cb]"><div className="w-full max-w-md text-center"><Logo light /><div className="mx-auto mt-20 grid h-28 w-28 place-items-center rounded-full border border-[#e8bd72]/30 bg-[#e8bd72]/10 animate-breathe"><Sparkles size={38} className="text-[#e8bd72]" /></div><h1 className="serif mt-10 text-4xl">Making this feel like yours.</h1><p className="mt-3 text-sm text-[#f6e9cb]/60">A thoughtful starting point, not a test you can fail.</p><div className="mt-12 space-y-3 text-left">{steps.map((text, i) => <div key={text} className={`flex items-center gap-3 rounded-xl px-4 py-3 text-sm transition-all ${i <= index ? 'bg-[#f6e9cb]/10 text-[#f6e9cb]' : 'text-[#f6e9cb]/30'}`}>{i < index ? <CheckCircle2 size={18} className="text-[#9fc9ad]" /> : <span className={`h-4 w-4 rounded-full border ${i === index ? 'animate-pulse border-[#e8bd72]' : 'border-[#f6e9cb]/20'}`} />}{text}</div>)}</div></div></div>;
}

export function Dashboard({ state, setState }: { state: FinovaState; setState: (s: FinovaState) => void }) {
  const nextId = state.roadmap.find(id => !state.completedMilestones.includes(id)) || state.roadmap[0]; const next = roadmapBase.find(x => x.id === nextId) || roadmapBase[0]; const nextLesson = lessons.find(x => x.id === next.lessonId) || lessons[0]; const completion = Math.round((state.completedMilestones.length / Math.max(1, state.roadmap.length)) * 100); const readiness = readinessScore(state); const first = state.name || 'there';
  return <ShellPage state={state} setState={setState}><PageIntro eyebrow={todayLabel()} title={`Good ${new Date().getHours() < 12 ? 'morning' : new Date().getHours() < 18 ? 'afternoon' : 'evening'}, ${first}.`} text="You do not need to solve everything today. One useful step is enough." action={<Link href="/profile" className="hidden items-center gap-2 text-sm font-semibold text-muted-foreground sm:flex"><span className="grid h-9 w-9 place-items-center rounded-full bg-secondary text-sm">{initials(state.name)}</span>{state.name}</Link>} /><div className="grid gap-5 lg:grid-cols-[1.4fr_.8fr]"><section className="relative overflow-hidden rounded-3xl bg-primary p-6 text-primary-foreground sm:p-8"><div className="absolute -right-12 -top-20 h-64 w-64 rounded-full border-[38px] border-primary-foreground/10" /><div className="relative"><p className="text-sm text-primary-foreground/70">Your recommended next step</p><h2 className="serif mt-3 max-w-md text-3xl leading-tight">{next.title}</h2><p className="mt-3 max-w-md text-sm leading-relaxed text-primary-foreground/70">{next.description}</p><div className="mt-7 flex flex-wrap gap-3"><Link href={`/lesson/${nextLesson.id}`} className="inline-flex items-center gap-2 rounded-xl bg-[#f6e9cb] px-4 py-2.5 text-sm font-semibold text-[#173b40]">{state.completedLessons.includes(nextLesson.id) ? 'Continue lesson' : 'Start lesson'} <ArrowRight size={16} /></Link><span className="inline-flex items-center gap-2 rounded-xl bg-primary-foreground/10 px-3 py-2.5 text-xs"><Clock3 size={14} />{nextLesson.minutes} min</span></div></div></section><section className="rounded-3xl border border-border bg-card p-6 card-shadow"><div className="flex items-start justify-between"><div><p className="text-sm text-muted-foreground">Learning readiness</p><p className="serif mt-2 text-5xl">{readiness}<span className="text-xl text-muted-foreground">/100</span></p></div><div className="grid h-11 w-11 place-items-center rounded-2xl bg-secondary text-primary"><Target size={21} /></div></div><ProgressBar value={readiness} /><p className="mt-3 text-sm text-muted-foreground">Based only on what you have learned and practiced in Finova—not your financial worth or health.</p></section></div><div className="mt-5 grid gap-5 lg:grid-cols-[1fr_1fr_.78fr]"><section className="rounded-3xl border border-border bg-card p-6 card-shadow"><div className="flex items-center justify-between"><h2 className="font-semibold">Your roadmap</h2><Link href="/roadmap" className="text-sm font-semibold text-primary">View all</Link></div><div className="mt-5 flex items-end gap-3"><span className="serif text-4xl">{state.completedMilestones.length}</span><span className="pb-1 text-sm text-muted-foreground">of {state.roadmap.length} milestones</span></div><ProgressBar value={completion} color="bg-accent" /></section><section className="rounded-3xl border border-border bg-card p-6 card-shadow"><div className="flex items-center justify-between"><h2 className="font-semibold">Mastery snapshot</h2><Link href="/progress" className="text-sm font-semibold text-primary">Details</Link></div><div className="mt-4 space-y-3">{Object.entries(state.mastery).slice(0, 3).map(([key, value]) => <div key={key}><div className="mb-1 flex justify-between text-xs"><span>{key}</span><span className="text-muted-foreground">{value}%</span></div><ProgressBar value={value} /></div>)}</div></section><section className="rounded-3xl border border-border bg-secondary/50 p-6"><div className="flex items-center gap-2 text-[#9b7040]"><BadgeCheck size={20} /><span className="text-xs font-bold uppercase tracking-wider">Learning rhythm</span></div><h2 className="serif mt-5 text-2xl">{state.streak ? `${state.streak} day streak.` : 'Start with one useful step.'}</h2><p className="mt-2 text-sm text-muted-foreground">{state.streak ? 'Consistency beats intensity here.' : 'Complete a lesson or simulation to begin tracking your rhythm.'}</p></section></div><section className="mt-5 rounded-3xl border border-border bg-card p-6 card-shadow"><h2 className="font-semibold">Recent activity</h2>{state.activity.length ? <div className="mt-4 grid gap-3 sm:grid-cols-3">{state.activity.slice(0,3).map((item,i)=><div key={`${item}-${i}`} className="flex items-center gap-3 rounded-2xl bg-muted/50 p-3 text-sm"><Check size={15}/><span>{item}</span></div>)}</div> : <p className="mt-4 text-sm text-muted-foreground">Your completed lessons and simulations will appear here.</p>}</section><div className="mt-5"><SafetyNote /></div></ShellPage>;
}

export function Roadmap({ state, setState }: { state: FinovaState; setState: (s: FinovaState) => void }) {
  const complete = state.completedMilestones.length;
  return <ShellPage state={state} setState={setState}><PageIntro eyebrow="YOUR MONEY MAP" title="90 levels. One financial life." text="Every topic grows from Beginner to Mastery. Finish a Beginner level to unlock that topic's Developing level and the next topic's Beginner level." />
  <div className="mb-6 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-5"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Curriculum</p><p className="serif mt-2 text-3xl">15 topics</p></div><div className="rounded-2xl border border-border bg-card p-5"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Depth</p><p className="serif mt-2 text-3xl">6 levels each</p></div><div className="rounded-2xl border border-border bg-card p-5"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Progress</p><p className="serif mt-2 text-3xl">{complete} / 90</p></div></div>
  <div className="space-y-5">{curriculumModules.map((module,moduleIndex)=>{const items=masteryLevels.map((level,levelIndex)=>roadmapBase.find(r=>r.moduleId===module.id&&r.level===level)!);const doneCount=items.filter(i=>state.completedMilestones.includes(i.id)).length;return <section key={module.id} className="rounded-3xl border border-border bg-card p-5 sm:p-7 card-shadow"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-secondary text-sm font-bold text-primary">{String(moduleIndex+1).padStart(2,'0')}</span><div><h2 className="text-xl font-semibold">{module.title}</h2><p className="mt-1 text-sm text-muted-foreground">{module.description}</p></div></div></div><span className="text-xs font-semibold text-muted-foreground">{doneCount} of 6 complete</span></div><div className="mt-5 grid gap-2 md:grid-cols-6">{items.map(item=>{const done=state.completedMilestones.includes(item.id);const unlocked=isMilestoneUnlocked(item.id,state.completedMilestones);return <Link key={item.id} href={unlocked?`/lesson/${item.lessonId}`:'/roadmap'} className={`relative rounded-2xl border p-3 transition-all ${done?'border-primary bg-primary text-primary-foreground':unlocked?'border-accent bg-secondary/55 hover:-translate-y-0.5':'border-border bg-muted/35 text-muted-foreground'}`}><div className="flex items-center justify-between"><span className="text-[10px] font-bold uppercase tracking-wider">{item.level}</span>{done?<Check size={14}/>:unlocked?<Play size={13}/>:<LockKeyhole size={13}/>}</div><p className="mt-3 text-xs leading-relaxed opacity-80">{item.description}</p>{unlocked&&!done&&<span className="mt-3 inline-block text-[10px] font-bold uppercase tracking-wider text-primary">Open</span>}</Link>})}</div></section>})}</div><div className="mt-6"><SafetyNote/></div></ShellPage>;
}
export function Learn({ state, setState }: { state: FinovaState; setState: (s: FinovaState) => void }) {
  const [query, setQuery] = useState(''); const [category, setCategory] = useState('All topics'); const filtered = lessons.filter(l => (category === 'All topics' || l.category === category) && `${l.title} ${l.description}`.toLowerCase().includes(query.toLowerCase())); return <ShellPage state={state} setState={setState}><PageIntro eyebrow="LEARNING LIBRARY" title="Learn at your own pace." text="Small lessons for the money questions that tend to show up at inconvenient times." /><div className="flex flex-col gap-3 md:flex-row"><div className="relative flex-1"><BookOpen className="absolute left-3 top-3.5 text-muted-foreground" size={17} /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search lessons…" className="w-full rounded-xl border border-input bg-card py-3 pl-10 pr-4 text-sm outline-none focus:ring-2 focus:ring-ring" data-testid="input-search-lessons" /></div><select value={category} onChange={e => setCategory(e.target.value)} className="rounded-xl border border-input bg-card px-4 py-3 text-sm outline-none" data-testid="select-lesson-category">{categories.map(c => <option key={c}>{c}</option>)}</select></div><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{filtered.map(lesson => <Link key={lesson.id} href={`/lesson/${lesson.id}`} className="group rounded-3xl border border-border bg-card p-5 card-shadow transition-transform hover:-translate-y-1" data-testid={`card-lesson-${lesson.id}`}><div className="flex items-start justify-between"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-secondary text-primary"><BookOpen size={20} /></span><span className="rounded-full bg-muted px-2.5 py-1 text-[11px] font-semibold text-muted-foreground">{lesson.category}</span></div><h2 className="mt-6 text-lg font-semibold">{lesson.title}</h2><p className="mt-2 min-h-10 text-sm leading-relaxed text-muted-foreground">{lesson.description}</p><div className="mt-5 flex items-center justify-between text-xs text-muted-foreground"><span className="flex items-center gap-1.5"><Clock3 size={14} />{lesson.minutes} min</span><span className="flex items-center gap-1 font-semibold text-primary">Open <ArrowRight size={14} className="transition-transform group-hover:translate-x-1" /></span></div></Link>)}</div>{filtered.length === 0 && <div className="mt-7 rounded-3xl border border-dashed border-border bg-card p-12 text-center"><Search className="mx-auto text-muted-foreground" /><p className="mt-4 font-semibold">Nothing matched that search.</p><p className="mt-1 text-sm text-muted-foreground">Try a broader phrase or choose All topics.</p></div>}<div className="mt-8"><SafetyNote /></div></ShellPage>;
}

export function Lesson({ state, setState }: { state: FinovaState; setState: (s: FinovaState) => void }) {
  const { lessonId } = useParams();
  const lesson = lessons.find(x => x.id === lessonId) || lessons[0];
  const content = lessonContent[lesson.id] || lessonContent['foundations'];
  const [screen, setScreen] = useState(0);
  const [selected, setSelected] = useState<number | null>(null);
  const [answers, setAnswers] = useState<[number | null, number | null]>([null, null]);
  const [result, setResult] = useState<{ score: number; delta: number; before: number; after: number; previousBest?: number }>({ score: 0, delta: 0, before: 0, after: 0 });
  const [, setLocation] = useLocation();
  const labels = ['Concept', 'Example', 'Check 1', 'Explanation', 'Scenario', 'Check 2', 'Results'];
  const checkIndex = screen === 2 ? 0 : 1;
  const currentCheck: KnowledgeCheck | undefined = screen === 2 || screen === 5 ? content.checks[checkIndex] : undefined;
  const previousBest = state.lessonBestScores[lesson.id];
  const rewardFor = (score: number) => [2, 5, 8][Math.max(0, Math.min(2, score))];
  const submitCheck = () => {
    if (selected === null || !currentCheck) return;
    const nextAnswers: [number | null, number | null] = [...answers] as [number | null, number | null];
    nextAnswers[checkIndex] = selected;
    setAnswers(nextAnswers);
    setSelected(null);
    if (checkIndex === 0) {
      setScreen(3);
      return;
    }
    const score = nextAnswers.filter((answer, index) => answer === content.checks[index].correct).length;
    const priorReward = previousBest === undefined ? 0 : rewardFor(previousBest);
    const delta = Math.max(0, rewardFor(score) - priorReward);
    const before = state.mastery[content.mastery] || 0;
    let next: FinovaState = {
      ...state,
      completedLessons: state.completedLessons.includes(lesson.id) ? state.completedLessons : [...state.completedLessons, lesson.id],
      lessonBestScores: { ...state.lessonBestScores, ...(previousBest === undefined || score > previousBest ? { [lesson.id]: score } : {}) },
      mastery: { ...state.mastery, [content.mastery]: Math.min(100, before + delta) },
    };
    const milestone = next.roadmap.map(id => roadmapBase.find(item => item.id === id)).find(item => item?.lessonId === lesson.id && !next.completedMilestones.includes(item.id));
    if (milestone) next = { ...next, completedMilestones: [...next.completedMilestones, milestone.id] };
    next = recordActivity(next, `You completed ${lesson.title}`);
    saveState(next);
    setState(next);
    setResult({ score, delta, before, after: next.mastery[content.mastery], previousBest });
    setScreen(6);
  };
  const explanationFor = (index: number) => {
    const answer = answers[index];
    const check = content.checks[index];
    return answer === check.correct
      ? { correct: true, title: 'Correct — here is why it works.', text: check.explanation }
      : { correct: false, title: 'That is not the best answer.', text: check.explanation };
  };
  const nextRoadmapItem = state.roadmap
    .map(id => roadmapBase.find(item => item.id === id))
    .find((item, index, list) => item?.lessonId === lesson.id && list[index + 1]);
  const nextHref = lesson.id === 'paycheck-taxes' ? '/practice/first-paycheck' : nextRoadmapItem?.lessonId ? `/lesson/${nextRoadmapItem.lessonId}` : '/roadmap';
  const nextLabel = lesson.id === 'paycheck-taxes' ? 'Practice this concept' : nextRoadmapItem ? 'Next lesson' : 'Continue roadmap';
  const screenTitle = screen === 0 ? lesson.title : screen === 1 ? content.exampleTitle : screen === 3 ? 'Why that answer matters' : screen === 4 ? content.scenarioTitle : screen === 6 ? 'Lesson complete' : content.checks[screen === 2 ? 0 : 1].prompt;
  const screenBody = screen === 0 ? content.opening : screen === 1 ? content.example : screen === 3 ? explanationFor(0).text : screen === 4 ? content.scenario : undefined;

  return <ShellPage state={state} setState={setState}><div className="mx-auto max-w-3xl"><Link href="/learn" className="mb-8 inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground"><ArrowLeft size={16}/>Back to lessons</Link><div className="mb-8 flex items-center justify-between"><span className="text-xs font-bold uppercase tracking-[.18em] text-primary">LESSON {screen + 1} OF 7</span><span className="text-xs text-muted-foreground">{lesson.minutes} min</span></div><ProgressBar value={(screen / 6) * 100}/><div className="mt-3 grid grid-cols-7 gap-1">{labels.map((label, index) => <div key={label} className={`h-1 rounded-full ${index <= screen ? 'bg-primary' : 'bg-muted'}`} title={label} />)}</div><section className="mt-10 rounded-3xl border border-border bg-card p-6 sm:p-10 card-shadow"><span className="grid h-14 w-14 place-items-center rounded-2xl bg-secondary text-primary"><BookOpen size={25}/></span><h1 className="serif mt-8 text-4xl leading-tight">{screenTitle}</h1>{screenBody && <p className="mt-5 text-lg leading-relaxed text-muted-foreground">{screenBody}</p>}{(screen === 2 || screen === 5) && currentCheck && <div className="mt-8 space-y-3">{currentCheck.choices.map((choice, index) => <button key={choice} onClick={() => setSelected(index)} className={`flex w-full items-center justify-between rounded-2xl border p-4 text-left text-sm ${selected === index ? 'border-primary bg-secondary' : 'border-border hover:bg-muted'}`}><span>{choice}</span><span className={`grid h-5 w-5 place-items-center rounded-full border ${selected === index ? 'border-primary bg-primary text-primary-foreground' : 'border-border'}`}>{selected === index && <Check size={13}/>}</span></button>)}{selected !== null && <Button className="mt-4" onClick={submitCheck}>Check my answer</Button>}</div>}{screen === 3 && <div className={`mt-6 rounded-2xl p-4 text-sm ${explanationFor(0).correct ? 'bg-[#d8e8de] text-[#173b40]' : 'bg-[#f5e3dc] text-[#6e3b31]'}`}><div className="flex items-center gap-2 font-semibold">{explanationFor(0).correct ? <CheckCircle2 size={18}/> : <X size={18}/>} {explanationFor(0).title}</div><p className="mt-2 opacity-80">{explanationFor(0).text}</p></div>}{screen === 6 && <div className="mt-6 space-y-5"><div className="rounded-2xl bg-secondary/50 p-4"><p className="text-sm text-muted-foreground">Knowledge checks</p><p className="mt-1 text-3xl font-semibold">{result.score} / 2</p><p className="mt-3 text-sm text-muted-foreground">{content.mastery} mastery: {result.before}% → {result.after}% {result.delta ? `(+${result.delta})` : '(no additional points this time)'}</p></div><div><h2 className="font-semibold">What you learned</h2><ul className="mt-3 space-y-2 text-sm text-muted-foreground">{content.takeaways.map(item => <li key={item} className="flex gap-2"><CheckCircle2 size={16} className="mt-0.5 shrink-0 text-primary"/><span>{item}</span></li>)}</ul></div><div className={`rounded-2xl p-4 text-sm ${answers[1] === content.checks[1].correct ? 'bg-[#d8e8de] text-[#173b40]' : 'bg-[#f5e3dc] text-[#6e3b31]'}`}><p className="font-semibold">{answers[1] === content.checks[1].correct ? 'Correct — here is why it works.' : 'That is not the best answer.'}</p><p className="mt-1 opacity-80">{content.checks[1].explanation}</p></div></div>}<div className="mt-10 flex flex-wrap justify-between gap-3"><OutlineButton onClick={() => { if (screen > 0 && screen < 6) { setScreen(screen - 1); setSelected(null); } }} disabled={screen === 0 || screen === 6}><ArrowLeft size={16}/>Back</OutlineButton>{screen === 0 && <Button onClick={() => setScreen(1)}>Continue <ArrowRight size={16}/></Button>}{screen === 1 && <Button onClick={() => setScreen(2)}>Start check <ArrowRight size={16}/></Button>}{screen === 3 && <Button onClick={() => setScreen(4)}>Continue <ArrowRight size={16}/></Button>}{screen === 4 && <Button onClick={() => setScreen(5)}>Final knowledge check <ArrowRight size={16}/></Button>}{screen === 6 && <><Link href="/roadmap" className="inline-flex items-center gap-2 rounded-xl border border-border bg-card px-4 py-2.5 text-sm font-semibold hover:bg-muted">Continue roadmap <ArrowRight size={16}/></Link><Link href={nextHref} className="inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">{nextLabel} <ArrowRight size={16}/></Link></>}</div></section><div className="mt-6"><SafetyNote/></div></div></ShellPage>;
}

export function Practice({ state, setState }: { state: FinovaState; setState: (s: FinovaState) => void }) {
 const scenarios=[
  ['First Paycheck','Turn a new salary into housing, retirement, spending and emergency decisions.','/practice/first-paycheck',true],
  ['Car Breaks Down','Handle a $1,200 repair without pretending every option is painless.','',false],
  ['First Credit Card','Use a card for one month, read the statement and decide what to pay.','',false],
  ['Moving Off Campus','Compare rent, utilities, deposits, roommates and transportation.','',false],
  ['Market Drop','Watch a hypothetical portfolio fall and decide what information matters next.','',false],
  ['Student Loan Repayment','Build a first post-college month around required loan payments.','',false],
  ['Choose Your Benefits','Balance take-home pay, a 401(k) match, health coverage and other benefits.','',false],
  ['Unexpected Medical Bill','Use insurance terms, cash flow and emergency savings in one decision.','',false],
 ];
 return <ShellPage state={state} setState={setState}><PageIntro eyebrow="PRACTICE LAB" title="Practice real life before it costs real money." text="Finova scenarios combine topics, force tradeoffs, and show the consequences of your decisions. More simulations unlock as the MoneyMap grows."/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{scenarios.map(([title,desc,href,live],i)=>{const card=<div className={`h-full rounded-3xl border p-6 transition-all ${live?'border-primary bg-primary text-primary-foreground hover:-translate-y-1':'border-border bg-card'}`}><div className="flex items-center justify-between"><span className={`grid h-10 w-10 place-items-center rounded-xl ${live?'bg-primary-foreground/10':'bg-secondary text-primary'}`}><Play size={18}/></span><span className={`rounded-full px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider ${live?'bg-[#e8bd72]/20 text-[#e8bd72]':'bg-muted text-muted-foreground'}`}>{live?'Play now':'Coming next'}</span></div><h2 className="serif mt-6 text-2xl">{title}</h2><p className={`mt-2 text-sm leading-relaxed ${live?'text-primary-foreground/70':'text-muted-foreground'}`}>{desc}</p><p className="mt-6 text-xs font-semibold">Scenario {i+1} · decisions + consequences</p></div>;return live?<Link key={title as string} href={href as string}>{card}</Link>:<div key={title as string}>{card}</div>})}</div><div className="mt-6 rounded-3xl bg-secondary/45 p-6"><h2 className="font-semibold">Where Practice Lab is going</h2><p className="mt-2 max-w-3xl text-sm leading-relaxed text-muted-foreground">Your decisions will eventually carry forward. Build an emergency fund in one scenario and it can protect you in the next. Add credit-card debt and you will have to deal with it later. The goal is not a fake perfect score—it is understanding consequences.</p></div><div className="mt-6"><SafetyNote/></div></ShellPage>;
}
export function FirstPaycheck({ state, setState }: { state: FinovaState; setState: (s: FinovaState) => void }) {
  const [step,setStep]=useState(0); const [prediction,setPrediction]=useState<number|null>(null); const [housing,setHousing]=useState(900); const [retirement,setRetirement]=useState(4); const [lifestyle,setLifestyle]=useState<'lean'|'balanced'|'high'>('balanced'); const [repairMethod,setRepairMethod]=useState<'savings'|'cash'|'card'|'family'|'delay'>('savings'); const [,setLocation]=useLocation();
  const grossMonthly=5417, takeHome=4100, startingSavings=1500, studentLoans=18000, repair=1200;
  const movingOut=housing!==400; const housingExtras=movingOut?225:0; const housingTotal=housing+housingExtras;
  const employee401k=Math.round(grossMonthly*retirement/100); const employerMatch=Math.round(grossMonthly*Math.min(retirement,4)/100);
  const essentials=800; const lifestyleSpend=lifestyle==='lean'?300:lifestyle==='balanced'?550:850;
  const preEmergencySurplus=takeHome-housingTotal-essentials-lifestyleSpend-employee401k;
  const cashAvailable=Math.max(0,preEmergencySurplus); const canCashCover=cashAvailable>=repair; const canSavingsCover=startingSavings>=repair;
  const emergencySavings=Math.max(0,startingSavings-(repairMethod==='savings'?repair:0));
  const cashAfterEmergency=preEmergencySurplus-(repairMethod==='cash'?repair:0);
  const cardDebt=repairMethod==='card'?repair:0; const familyDebt=repairMethod==='family'?repair:0;
  const unresolvedCost=repairMethod==='delay'?repair:0; const retirementTotal=employee401k+employerMatch;
  const matchPotential=Math.round(grossMonthly*.04); const matchMissed=Math.max(0,matchPotential-employerMatch);
  const housingPct=Math.round(housingTotal/takeHome*100); const savingsRate=Math.max(0,Math.round((employee401k+Math.max(0,preEmergencySurplus))/takeHome*100));
  const nextMonthStartingCash=Math.max(0,cashAfterEmergency); const runway=Math.round((emergencySavings/Math.max(1,housingTotal+essentials))*10)/10;
  const steps=['Paycheck','Housing','Retirement','Spending','Surprise','Response','Report'];
  const finish=()=>{ let next: FinovaState={...state,simulations:state.simulations+1,mastery:{...state.mastery,Taxes:Math.min(100,(state.mastery.Taxes||0)+10),Retirement:Math.min(100,(state.mastery.Retirement||0)+8),Saving:Math.min(100,(state.mastery.Saving||0)+8),Budgeting:Math.min(100,(state.mastery.Budgeting||0)+6)},financialProfile:{cash:nextMonthStartingCash,emergencyFund:emergencySavings,creditCardDebt:cardDebt,studentLoans,retirement:retirementTotal,employerMatch,monthlySurplus:cashAfterEmergency}}; next=recordActivity(next,'You completed First Paycheck: real-life month'); saveState(next); setState(next); setLocation('/progress'); };
  const option=(active:boolean)=>`rounded-2xl border p-4 text-left transition ${active?'border-primary bg-secondary':'border-border hover:bg-muted'}`;
  return <ShellPage state={state} setState={setState}><div className="mx-auto max-w-4xl"><Link href="/practice" className="mb-8 inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground"><ArrowLeft size={16}/>Practice lab</Link><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-primary">FIRST PAYCHECK · LIFE SIMULATION</p><h1 className="serif mt-2 text-3xl sm:text-4xl">Your first month after graduation.</h1><p className="mt-2 max-w-2xl text-sm text-muted-foreground">Your choices carry forward. There is no perfect score—the goal is to see what each decision changes.</p></div><span className="shrink-0 text-sm text-muted-foreground">{step+1} / 7</span></div><div className="mt-5 grid grid-cols-7 gap-1.5">{steps.map((x,i)=><div key={x}><div className={`h-1.5 rounded-full ${i<=step?'bg-primary':'bg-muted'}`}/><span className="mt-1 hidden text-[10px] text-muted-foreground md:block">{x}</span></div>)}</div>
  {step<6?<div className="mt-8 rounded-3xl border border-border bg-card p-6 sm:p-9 card-shadow"><p className="text-sm font-semibold text-primary">Decision {step+1}</p>
  {step===0&&<><h2 className="serif mt-3 text-3xl">Your offer says $65,000. What hits checking?</h2><p className="mt-3 text-sm leading-relaxed text-muted-foreground">That salary is about $5,417 gross each month. Taxes and deductions mean gross pay is not spendable pay.</p><div className="mt-7 grid gap-3 sm:grid-cols-4">{[5200,4100,3500,0].map(v=><button key={v} onClick={()=>setPrediction(v)} className={option(prediction===v)}><strong>{v?`$${v.toLocaleString()}`:'Not sure'}</strong></button>)}</div>{prediction!==null&&<div className="mt-6 rounded-2xl bg-secondary/50 p-4 text-sm"><strong>Simulation take-home: ≈ $4,100/month.</strong><p className="mt-1 text-muted-foreground">Actual take-home varies by location, taxes, benefits, withholding and retirement elections. The important distinction is gross vs. take-home.</p></div>}</>}
  {step===1&&<><h2 className="serif mt-3 text-3xl">Choose where you live.</h2><p className="mt-3 text-sm text-muted-foreground">Housing affects every later choice. Moving out also adds a simplified $225 for utilities, internet and renter’s insurance.</p><div className="mt-7 grid gap-3 sm:grid-cols-2">{[[900,'Roommates'],[1350,'Studio'],[1850,'One-bedroom'],[400,'Live with family']].map(([v,l])=><button key={v} onClick={()=>setHousing(Number(v))} className={option(housing===v)}><strong>{l}</strong><span className="mt-1 block text-sm text-muted-foreground">${v}/month{v===400?' household contribution':' rent'}</span></button>)}</div><div className="mt-5 rounded-2xl bg-secondary/50 p-4 text-sm">Housing total: <strong>${housingTotal.toLocaleString()}/mo</strong> · about <strong>{housingPct}%</strong> of simulated take-home.</div></>}
  {step===2&&<><h2 className="serif mt-3 text-3xl">Your employer matches the first 4%.</h2><p className="mt-3 text-sm text-muted-foreground">Choose a contribution. This reduces spendable cash today while building retirement savings; the employer match is additional compensation in this simulation.</p><div className="mt-7 grid grid-cols-4 gap-3">{[0,2,4,8].map(v=><button key={v} onClick={()=>setRetirement(v)} className={option(retirement===v)}><strong>{v}%</strong></button>)}</div><div className="mt-5 rounded-2xl bg-secondary/50 p-4 text-sm">You contribute <strong>${employee401k}/mo</strong> · employer adds <strong>${employerMatch}/mo</strong>{matchMissed>0&&<> · <strong>${matchMissed}/mo</strong> of available match is not captured.</>}</div></>}
  {step===3&&<><h2 className="serif mt-3 text-3xl">Now live the month.</h2><p className="mt-3 text-sm text-muted-foreground">After housing, assume $800 for groceries, transportation, phone and other essentials. Choose the rest of your lifestyle spending.</p><div className="mt-7 grid gap-3 sm:grid-cols-3">{([['lean','Lean month','$300'],['balanced','Balanced','$550'],['high','Higher spending','$850']] as const).map(([v,l,a])=><button key={v} onClick={()=>setLifestyle(v)} className={option(lifestyle===v)}><strong>{l}</strong><span className="mt-1 block text-sm text-muted-foreground">{a} discretionary</span></button>)}</div><div className={`mt-5 rounded-2xl p-4 text-sm ${preEmergencySurplus>=0?'bg-[#d8e8de] text-[#173b40]':'bg-[#f5e3dc] text-[#6e3b31]'}`}>Before any surprise, your month is <strong>{preEmergencySurplus>=0?`+$${preEmergencySurplus.toLocaleString()} surplus`:`-$${Math.abs(preEmergencySurplus).toLocaleString()} short`}</strong>.</div></>}
  {step===4&&<><h2 className="serif mt-3 text-3xl">Then real life happens.</h2><div className="mt-6 rounded-3xl bg-[#173b40] p-6 text-[#f6e9cb]"><p className="text-xs font-bold uppercase tracking-wider text-[#e8bd72]">Unexpected expense</p><p className="serif mt-3 text-4xl">$1,200 car repair</p><p className="mt-3 text-sm text-[#f6e9cb]/70">The car is needed to get to work. You entered the month with $1,500 in emergency savings and currently have ${Math.max(0,cashAvailable).toLocaleString()} of monthly cash available.</p></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl bg-muted p-4"><p className="text-xs text-muted-foreground">Emergency fund</p><p className="mt-1 text-2xl font-semibold">$1,500</p><p className="mt-1 text-xs text-muted-foreground">{canSavingsCover?'Enough to cover the repair':'Not enough for the full repair'}</p></div><div className="rounded-2xl bg-muted p-4"><p className="text-xs text-muted-foreground">Cash from this month</p><p className="mt-1 text-2xl font-semibold">${cashAvailable.toLocaleString()}</p><p className="mt-1 text-xs text-muted-foreground">{canCashCover?'Enough to pay from monthly cash':'Not enough to pay the full repair'}</p></div></div></>}
  {step===5&&<><h2 className="serif mt-3 text-3xl">How do you handle the repair?</h2><p className="mt-3 text-sm text-muted-foreground">The choice changes what follows you into next month.</p><div className="mt-7 grid gap-3">{([['savings','Use emergency savings',canSavingsCover?'Covers it; emergency fund falls to $300.':'Not enough savings.'],['cash','Pay from this month’s cash',canCashCover?'Covers it without new debt.':'Your monthly cash is not enough; this would create a shortfall.'],['card','Put it on a credit card','Preserves cash today but creates a $1,200 balance that can accrue interest.'],['family','Borrow from family','Avoids card interest but still creates a $1,200 obligation.'],['delay','Delay the repair','Preserves money today, but the transportation problem remains unresolved.']] as const).map(([v,l,d])=><button key={v} disabled={(v==='cash'&&!canCashCover)||(v==='savings'&&!canSavingsCover)} onClick={()=>setRepairMethod(v)} className={`${option(repairMethod===v)} disabled:cursor-not-allowed disabled:opacity-45`}><strong>{l}</strong><span className="mt-1 block text-sm text-muted-foreground">{d}</span></button>)}</div></>}
  </div>:<div className="mt-8 space-y-5"><div className="rounded-3xl bg-primary p-6 text-primary-foreground sm:p-9"><BadgeCheck size={28}/><p className="mt-5 text-xs font-bold uppercase tracking-[.18em] text-primary-foreground/60">MONTH COMPLETE</p><h2 className="serif mt-2 text-4xl">Here is what your decisions actually changed.</h2><p className="mt-3 max-w-2xl text-sm text-primary-foreground/70">Not a financial-health score. These are the simulated consequences that would follow you into the next month.</p></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[['Ending monthly cash',cashAfterEmergency<0?`-$${Math.abs(cashAfterEmergency).toLocaleString()}`:`$${cashAfterEmergency.toLocaleString()}`],['Emergency fund',`$${emergencySavings.toLocaleString()}`],['New card debt',`$${cardDebt.toLocaleString()}`],['401(k) + match',`$${retirementTotal.toLocaleString()}`],['Employer match missed',`$${matchMissed.toLocaleString()}`],['Housing / take-home',`${housingPct}%`],['Savings rate',`${savingsRate}%`],['Emergency runway',`${runway} mo`]].map(([l,v])=><div key={l} className="rounded-2xl border border-border bg-card p-4 card-shadow"><p className="text-xs text-muted-foreground">{l}</p><p className="mt-2 text-2xl font-semibold">{v}</p></div>)}</div><div className="grid gap-4 lg:grid-cols-2"><div className="rounded-3xl border border-border bg-card p-6"><h3 className="font-semibold">What happened when the car broke down</h3><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{repairMethod==='savings'?'Your emergency fund absorbed the $1,200 repair. You avoided new debt, but your buffer fell to $300 and would need rebuilding.':repairMethod==='cash'?`Your monthly surplus covered the repair. You avoided new debt and kept the $1,500 emergency fund, but only $${Math.max(0,cashAfterEmergency).toLocaleString()} of monthly cash remains.`:repairMethod==='card'?'You kept your cash and emergency fund intact, but a $1,200 credit-card balance now follows you into next month and may accrue interest if not paid under the card terms.':repairMethod==='family'?'You kept your emergency fund intact and avoided card interest, but you now owe family $1,200. The obligation is still part of your financial picture.':'You preserved cash and savings, but the $1,200 transportation problem is unresolved. Delaying an essential repair can create work or safety consequences.'}</p></div><div className="rounded-3xl border border-border bg-secondary/45 p-6"><h3 className="font-semibold">If this were next month…</h3><p className="mt-3 text-sm leading-relaxed text-muted-foreground">You would begin with <strong>${nextMonthStartingCash.toLocaleString()} in leftover monthly cash</strong>, <strong>${emergencySavings.toLocaleString()} in emergency savings</strong>{cardDebt?`, and $${cardDebt.toLocaleString()} in new card debt`:''}{familyDebt?`, plus a $${familyDebt.toLocaleString()} family obligation`:''}{unresolvedCost?`, with the $${unresolvedCost.toLocaleString()} repair still unresolved`:''}. Your student-loan balance in this scenario remains <strong>${studentLoans.toLocaleString()}</strong>.</p></div></div><div className="rounded-3xl border border-border bg-card p-6"><h3 className="font-semibold">Decision signals</h3><div className="mt-4 grid gap-3 sm:grid-cols-2">{housingPct>40&&<p className="rounded-xl bg-[#f5e3dc] p-3 text-sm text-[#6e3b31]">Housing takes {housingPct}% of simulated take-home, leaving less flexibility for everything else.</p>}{matchMissed>0&&<p className="rounded-xl bg-[#f0dfbc]/70 p-3 text-sm">Your contribution left ${matchMissed}/month of the simulated employer match uncaptured.</p>}{emergencySavings<500&&<p className="rounded-xl bg-[#f0dfbc]/70 p-3 text-sm">Your emergency buffer is now thin. A second surprise could be harder to absorb without borrowing.</p>}{cardDebt>0&&<p className="rounded-xl bg-[#f5e3dc] p-3 text-sm text-[#6e3b31]">The repair became revolving debt. The next decision would be how aggressively to repay it.</p>}{preEmergencySurplus>0&&<p className="rounded-xl bg-[#d8e8de] p-3 text-sm text-[#173b40]">Before the emergency, your choices created a ${preEmergencySurplus.toLocaleString()} monthly surplus.</p>}{retirement>=4&&<p className="rounded-xl bg-[#d8e8de] p-3 text-sm text-[#173b40]">Your selected contribution captured the full simulated 4% employer match.</p>}</div></div></div>}
  <div className="mt-5 flex justify-between"><OutlineButton onClick={()=>setStep(Math.max(0,step-1))} disabled={step===0}><ArrowLeft size={16}/>Back</OutlineButton>{step<6?<Button onClick={()=>setStep(step+1)} disabled={step===0&&prediction===null}>Next <ArrowRight size={16}/></Button>:<Button onClick={finish}>Save this month <TrendingUp size={16}/></Button>}</div><div className="mt-6"><SafetyNote/></div></div></ShellPage>;
}
export function Progress({ state, setState }: { state: FinovaState; setState: (s: FinovaState) => void }) { const values=Object.values(state.mastery); const average=Math.round(values.reduce((a,b)=>a+b,0)/Math.max(1,values.length)); const readiness=readinessScore(state); const change=readiness-state.baselineReadiness; const weakest=Object.entries(state.mastery).sort((a,b)=>a[1]-b[1])[0]; const recommended=lessons.find(l=>(l.category==='Money Basics'?'Budgeting':l.category)===weakest?.[0])||lessons[0]; return <ShellPage state={state} setState={setState}><PageIntro eyebrow="YOUR PROGRESS" title="Evidence that you are learning." text="These numbers reflect activity inside Finova—not your financial worth or financial health."/><div className="grid gap-5 md:grid-cols-3"><div className="rounded-3xl bg-primary p-6 text-primary-foreground"><p className="text-sm text-primary-foreground/65">Learning readiness</p><p className="serif mt-2 text-5xl">{readiness}</p><p className="mt-4 text-sm text-primary-foreground/65">{change>0?`Up ${change} points since onboarding`:'Complete a lesson or simulation to move this score.'}</p></div><div className="rounded-3xl border border-border bg-card p-6 card-shadow"><p className="text-sm text-muted-foreground">Lessons completed</p><p className="serif mt-2 text-5xl">{state.completedLessons.length}</p></div><div className="rounded-3xl border border-border bg-card p-6 card-shadow"><p className="text-sm text-muted-foreground">Practice runs</p><p className="serif mt-2 text-5xl">{state.simulations}</p></div></div><div className="mt-5 grid gap-5 lg:grid-cols-[1.25fr_.75fr]"><section className="rounded-3xl border border-border bg-card p-6 card-shadow"><div className="flex items-center justify-between"><h2 className="font-semibold">Mastery by topic</h2><span className="text-sm text-muted-foreground">{average}% average</span></div><div className="mt-7 space-y-5">{Object.entries(state.mastery).map(([key,value])=><div key={key}><div className="mb-2 flex justify-between text-sm"><span>{key}</span><span className="font-semibold">{value}%</span></div><ProgressBar value={value}/></div>)}</div></section><section className="rounded-3xl border border-border bg-secondary/45 p-6"><div className="flex items-center gap-2 text-primary"><Flame size={19}/><span className="text-sm font-semibold">{state.streak} day streak</span></div><h2 className="serif mt-6 text-3xl">Recommended focus: {weakest?.[0] || 'Foundations'}</h2><p className="mt-3 text-sm leading-relaxed text-muted-foreground">Your lowest current mastery area is a useful place to continue—not a judgment.</p><Link href={`/lesson/${recommended.id}`} className="mt-7 inline-flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Open recommended lesson <ArrowRight size={16}/></Link></section></div><div className="mt-5 rounded-3xl border border-border bg-card p-6 card-shadow"><h2 className="font-semibold">Recent learning activity</h2>{state.activity.length?<div className="mt-4 space-y-2">{state.activity.map((a,i)=><div key={i} className="rounded-xl bg-muted/50 p-3 text-sm">{a}</div>)}</div>:<p className="mt-4 text-sm text-muted-foreground">No activity yet. Your history will appear here as you learn.</p>}</div></ShellPage>; }

export function Glossary({ state, setState }: { state: FinovaState; setState: (s: FinovaState) => void }) { const [query, setQuery] = useState(''); const list = glossary.filter(x => `${x.term} ${x.definition}`.toLowerCase().includes(query.toLowerCase())); return <ShellPage state={state} setState={setState}><PageIntro eyebrow="PLAIN-LANGUAGE GLOSSARY" title="Money words, without the wall of jargon." text="Search a term, get the useful version, and move on with your day." /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search a definition…" className="w-full rounded-xl border border-input bg-card px-4 py-3 text-sm outline-none focus:ring-2 focus:ring-ring" data-testid="input-search-glossary" /><div className="mt-6 grid gap-3">{list.map(item => <details key={item.term} className="group rounded-2xl border border-border bg-card p-5 card-shadow"><summary className="flex cursor-pointer list-none items-center justify-between font-semibold"><span>{item.term}</span><ChevronDown size={18} className="text-muted-foreground transition-transform group-open:rotate-180" /></summary><p className="mt-4 text-sm leading-relaxed text-muted-foreground">{item.definition}</p><p className="mt-3 rounded-xl bg-secondary/50 p-3 text-xs leading-relaxed"><strong>Why it matters:</strong> {item.why}</p></details>)}</div></ShellPage>; }

export function Profile({ state, setState }: { state: FinovaState; setState: (s: FinovaState) => void }) { const [,setLocation]=useLocation(); const update=(patch:Partial<FinovaState>)=>{const next={...state,...patch};saveState(next);setState(next)}; const reset=()=>{localStorage.removeItem('finova-onboarding-draft');saveState(seedState);setState(seedState);setLocation('/onboarding');}; return <ShellPage state={state} setState={setState}><PageIntro eyebrow="PROFILE & SETTINGS" title="Keep Finova feeling like yours." text="Your prototype profile is stored only in this browser."/><div className="grid gap-5 lg:grid-cols-[.75fr_1.25fr]"><section className="rounded-3xl bg-primary p-7 text-primary-foreground"><div className="grid h-16 w-16 place-items-center rounded-full bg-[#e8bd72] text-xl font-bold text-[#173b40]">{initials(state.name)}</div><h2 className="serif mt-7 text-3xl">{state.name || 'Your profile'}</h2><p className="mt-2 text-sm text-primary-foreground/65">{state.stage || 'Financial learner'}</p><div className="mt-8 rounded-2xl bg-primary-foreground/10 p-4"><p className="text-xs text-primary-foreground/65">Current goal</p><p className="mt-2 text-sm">{state.goal || 'Build financial confidence'}</p></div></section><section className="rounded-3xl border border-border bg-card p-6 card-shadow"><h2 className="font-semibold">Your learning profile</h2><div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl bg-muted p-4"><p className="text-xs text-muted-foreground">Savings snapshot</p><p className="mt-2 text-xl font-semibold">${state.savings.toLocaleString()}</p></div><div className="rounded-2xl bg-muted p-4"><p className="text-xs text-muted-foreground">Money confidence</p><p className="mt-2 text-xl font-semibold">{state.confidence || '—'} / 5</p></div></div><div className="mt-6"><p className="text-sm font-semibold">Topics on your mind</p><div className="mt-3 flex flex-wrap gap-2">{state.interests.length?state.interests.map(topic=><span key={topic} className="rounded-full bg-secondary px-3 py-1.5 text-xs font-semibold">{topic}</span>):<span className="text-sm text-muted-foreground">Retake the assessment to choose topics.</span>}</div></div><div className="mt-7 border-t border-border pt-6"><h3 className="text-sm font-semibold">Preferences</h3><label className="mt-4 flex items-center justify-between rounded-xl bg-muted/60 p-3 text-sm"><span>Weekly learning nudge</span><input type="checkbox" checked={state.preferences.weeklyNudge} onChange={e=>update({preferences:{...state.preferences,weeklyNudge:e.target.checked}})} className="h-4 w-4 accent-[#1c766e]"/></label><label className="mt-2 flex items-center justify-between rounded-xl bg-muted/60 p-3 text-sm"><span>Show practical examples first</span><input type="checkbox" checked={state.preferences.practicalFirst} onChange={e=>update({preferences:{...state.preferences,practicalFirst:e.target.checked}})} className="h-4 w-4 accent-[#1c766e]"/></label></div><div className="mt-7 flex flex-wrap gap-3"><Link href="/onboarding" className="inline-flex items-center gap-2 rounded-xl border border-border px-4 py-2.5 text-sm font-semibold hover:bg-muted"><RotateCcw size={16}/>Retake assessment</Link><OutlineButton onClick={reset}><RotateCcw size={16}/>Reset demo</OutlineButton></div></section></div><div className="mt-6"><SafetyNote/></div></ShellPage>; }